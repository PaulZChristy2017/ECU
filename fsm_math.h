#include <stdlib.h>
#include "boolean.h"
#ifndef  fsm_math_h
#define fsm_math_h

char myinput[100], op, temp[20];

float ans, num1, num2;

boolean is_digit(char);
boolean is_op(char);
boolean is_space(char);

#endif
